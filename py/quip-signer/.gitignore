# Built PyO3 extension (generated by `make py-signer` / maturin), like the
# browser signer's .wasm. The package source (__init__.py, .pyi, py.typed) is
# tracked; the compiled artifact is not.
quip_signer/*.so
quip_signer/*.pyd
quip_signer/*.dylib
__pycache__/
*.egg-info/
